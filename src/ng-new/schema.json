{
  "$schema": "http://json-schema.org/schema",
  "id": "AngularShellSchematicNgNew",
  "title": "Create an angular shell workspace",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the workspace.",
      "type": "string",
      "format": "html-selector",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the workspace?"
    },
    "directory": {
      "type": "string",
      "format": "path",
      "description": "The directory name to create the workspace in.",
      "default": ""
    },
    "title": {
      "type": "string",
      "default": ""
    },
    "prefix": {
      "type": "string",
      "default": ""
    }
  }
}
